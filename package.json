{
  "name": "splat-viewer",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Browser viewer + camera-pose editor for Gaussian splats (.sog / .ply). Load a splat, fly around, capture camera poses, preview the motion between them, and export poses.json.",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "playcanvas": "^2.20.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.9.0",
    "vite": "^6.0.0"
  }
}
